const isActive = true;
const hasPermission = false;

//* Conversion implicita
const result = 5 > 3;
// console.log(result);

const names = "hola";
// console.log(!!names); //! Identifica si hay algo / convertir un valor a su equivalente booleano

const isHere = true;
// console.log(!isHere);

//* conversion explicita
const value = 0;
const otherValue = -24;
const explicitBoolean = Boolean(value);
console.log(explicitBoolean);
